key: opcode_detector_xgboost
name: Opcode Detector XGBoost
version: "1.0.0"
tasks: [train, predict]
inputs:
    dataset: directory  
outputs:
    model: file
    metrics: json
    predictions: csv
paramsDefault:
    n_estimators: 100
    max_depth: 6
    learning_rate: 0.1
    use_label_encoder: false
    random_state: 42
    test_size: 0.3
